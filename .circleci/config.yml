version: 2
jobs:
  test:
    docker:
      - image: circleci/node:8.12
    steps:
      - restore_cache:
          name: Restore Yarn cache for client and server
          keys:
            - yarn-packages-{{ checksum "yarn.lock" }}
      - run:
          name: Install Dependencies
          command: yarn
      - run:
          name: Run tests
          command: yarn test
  build:
    docker:
      - image: circleci/node:8.12
    steps:
      - setup_remote_docker
      - run:
          name: Deploy all connectors
          command: |
            base64 --decode $DEPLOYMENT_CONFIG_JS > deployment.config.js
            BRANCH=$CIRCLE_BRANCH
            CONNECTORS=`sh scripts/get-connectors-to-deploy.sh`
            echo $CONNECTORS
workflows:
  version: 2
  build_and_test:
    jobs:
      - checkout
      - test:
          requires:
            - checkout
      - build:
          requires:
            - checkout
          filters:
            branches:
              only:
                - master
                - develop
