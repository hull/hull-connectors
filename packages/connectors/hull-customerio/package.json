{
  "name": "hull-customerio",
  "description": "Sync users within customer.io service",
  "version": "v0.0.18-monorepo",
  "scripts": {
    "test": "exit 0; echo 'tests are run by global jest'",
    "documentation": "exit 0"
  },
  "dependencies": {
    "hull": "link:../../hull",
    "object-hash": "^1.3.0",
    "superagent-prefix": "^0.0.2",
    "superagent-throttle": "^0.2.5"
  },
  "devDependencies": {
    "assert": "^1.4.1"
  }
}
