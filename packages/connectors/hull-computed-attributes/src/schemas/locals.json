{
  "name": "locals",
  "type": "array",
  "default": [{
    "target": "tracking_id",
    "source": "$split(user.external_id,/1/)[1]"
  }],
  "items": {
    "type": "object",
    "required": ["target", "source"],
    "properties": {
      "target": {
        "title": "Variable Name",
        "type": "string",
        "pattern": "^[/a-zA-Z_\\-0-9]+$"
      },
      "source": {
        "title": "Expression",
        "type": "string"
      }
    }
  }
}